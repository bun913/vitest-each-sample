import { HouseAllowance } from "./housingAllowance"

describe("HouseAllowance", () => {
  it("retruns 10000 yen when the distance is less than 20000m", () => {})
  it("returns 5000 yen when the distance is less than 50000m", () => {})
  it("returns 0 yen when the distance is more than 50000m", () => {})
})
